const VACATION_FIELDS = ['description', 'destination', 'img', 'start_date', 'finish_date', 'price'];
const VACATION_DELETE_BY = ['id'];
const VACATION_EDIT_FIELDS = ['description', 'destination', 'img', 'start_date', 'finish_date', 'price', 'id'];

const LOGIN_FIELDS = ['username', 'password'];

const USER_FIELDS = ['username', 'password', 'first_name', 'last_name'];

module.export = {
    VACATION_FIELDS,
    VACATION_DELETE_BY,
    VACATION_EDIT_FIELDS,
    LOGIN_FIELDS,
    USER_FIELDS
}